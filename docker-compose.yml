services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: btcfi
      POSTGRES_PASSWORD: btcfi
      POSTGRES_DB: btcfi
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
