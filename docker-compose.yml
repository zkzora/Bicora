services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: bicora
      POSTGRES_PASSWORD: bicora
      POSTGRES_DB: bicora
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
